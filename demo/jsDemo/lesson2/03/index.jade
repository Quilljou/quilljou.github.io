//output: index.html
<!DOCTYPE html>
html(lang="en")
head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    title 求出数组中所有数字的和
    style.
        .container {
            width: 50%;
            margin: 0 auto;
        }
        .line-1 input{
            float: left;
        }
body
    .container
        .line-1
            input#input(type="text")
            p 输入数字求和，数字之间用半角','分隔
        input#btn(type="button" value="求和")
        #result
    script.
        window.onload = function() {
            var btn = document.getElementById('btn');
            btn.onclick = function(){
                var input = document.getElementById('input').value;
                var inputArr = input.split(',');
                var result = document.getElementById('result');
                if (input) {
                    var sum = 0;
                    inputArr.forEach(function(item,index,html){
                        item = Number(item);
                        sum += item;
                        result.innerHTML = '<strong style="color:red">'+ sum + '</strong>';
                    })
                }
            }
        }
