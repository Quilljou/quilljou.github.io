// output: index.html

<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title 自定义右键菜单
        style.
            ul {
                margin: 0;
                padding: 0;
                overflow: hidden;
                width: 80px;
            }
            li {
                border: 1px solid #ce82c6;
                list-style: none;
                height: 25px;
                padding: 0;
                text-align: center;
            }
            li:nth-of-type(2){
                border-top: none;
                border-bottom: none;
            }
            span {
                width: 20px;
                display: inline-block;
                height: 100%;
                float: left;
                background: rgb(217, 186, 118);
                color: snow;
            }
    body
        ul(style="display:none")
            li <span>✄</span>剪切
            li <span>✎</span>复制
            li <span>▤</span>粘贴
        
        script.
            document.oncontextmenu = function(event) {
                var ul = document.getElementsByTagName('ul')[0];
                event.preventDefault();
                ul.style.display = 'block';
                ul.style.position = 'absolute';
                ul.style.top = event.pageY + 5 +'px';
                ul.style.left = event.clientX + 5 + 'px';
                
                this.onclick = function() {
                    ul.style.display = 'none';
                }
            }
            
            
